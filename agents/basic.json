{
  "name": "basic",
  "description": "A basic agent with bash access for testing the runner.",
  "system": "You are a helpful assistant. Use the bash tool to execute commands when needed. Be concise.",
  "model": "claude-sonnet-4-5-20250514",
  "processes": [
    {
      "name": "bash",
      "description": "Execute a shell command and return its output.",
      "input_schema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The shell command to execute"
          }
        },
        "required": ["command"]
      }
    }
  ]
}
